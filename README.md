# About element sentence constructor

This code will allow you to type a string, and let your system try to match the words with symbols to form a sentence.

The result will only show when it is able to match all letters.
The code will only scan the alphabetical characters. All other characters are ignored.